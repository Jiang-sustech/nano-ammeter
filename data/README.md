# 实测数据归档

> 采集于 **2026-09-13**，固件为 `physics_exp_test`（标定那版 = `git show HEAD~n:...`
> 见根 README 的"固件版本"一节）。环境：2636B 源表 + 自制裁板 + CH340 串口。

## 一、标定数据（36 个 `raw_09_13_*.npz`）

**12 个电流点 × 3 次重复**，正负兼备，覆盖 ±2 ~ ±40 nA。

| 点 | 板子读数 | 2636B 回读 | 误差 |
|---|---|---|---|
| +40 | +40.424 | 39.9989 | +1.06% |
| +30 | +30.314 | 29.9987 | +1.05% |
| +20 | +20.196 | 19.9968 | +1.00% |
| +10 | +10.102 | 9.9940 | +1.08% |
| +5 | +5.050 | 4.9940 | +1.12% |
| +2 | +2.016 | 1.9918 | +1.22% |
| −2 | −2.032 | −2.0055 | −1.32% |
| −5 | −5.066 | −5.0060 | −1.20% |
| −10 | −10.124 | −10.0072 | −1.17% |
| −20 | −20.211 | −20.0095 | −1.01% |
| −30 | −30.329 | −30.0110 | −1.06% |
| −40 | −40.434 | −40.0118 | −1.06% |

**注意几个记录上的坑**（都在采集过程中踩到并修正）：

- `--true=` 必须在采集时打对。有 **3 个点事后改过 `true_na`**：`+2`（1.9915→1.9918）、
  `−10`（−10.0070→−10.0072）、`−30`（−30.112→−30.0110，**是把 0 抄成了 1**，
  对应的 3 个 npz 是重采的那一批，旧的一批已删）。
- **2636B 输出全程是开着的**。中途有一次误以为源没开，实际那是输入开路导致的
  假漂移。

## 二、派生结果

| 文件 | 内容 | 生成方式 |
|---|---|---|
| `fit_data.csv` | 36 行，`I_true,c1,c2,m,n` —— 三常数最小二乘的输入 | `tools/make_fit_csv.py` |
| `linearity.png` | 图 4-2：未校准输入—输出关系 + 残差（对应 4.3.3） | `tools/fit_linearity.py` |
| `residual_fit.png` | 三常数最小二乘的 2×3 图（含残差比例直方图）。RMSE 1.5 pA、N=36 | `tools/fit_constants.m` |
| `M3_waveform.png` | M3 高阻↔+5V 交替波形（用于量寄生电容 `C_p`） | 一次性调查 |
| `node_decay_sim.png` | 100 MΩ 输入端节点的电压仿真（判断示波器探头电容的影响） | `tools/sim_node_decay.py` |
| `badread_M0.png` | M0（参考断开）整窗：6250 点里**只有 i=0 一个坏读**（值 = 0），及其 ±50 点放大 | 一次性调查 |
| `badread_first_sample.png` | 同上，并排 M0 与 M3（400 点里 31 个坏读）。用于定性"首拍坏读" | 一次性调查 |
| `demo_hist.png` | **原型图**：修正前/修正后两张残差直方图 + 误差曲线 + 输入输出 | 一次性原型 |

> `demo_hist.png` 的用途已被 `tools/sweep_broad.py` 的 `sweep_broad_fit.png` 取代
> （后者对同一口径做了正式实现），此处只作留档。
>
> 标了"一次性调查/原型"的三张**没有生成脚本**，是当时用临时命令画出来判断问题的。
> 结论已进 `docs/`，图留在这里备查；要重画得照结论重写脚本。

## 二·补、2026-09-15 标定扫描（自动标定第一次跑通）

`cal_sweep_raw.csv` / `cal_sweep_fit.csv` —— 19 点 × 3 次，+45 → −45 nA 步长 5。
**这是本工程第一次跑通自动标定**（之前 36 点数据是手动设源表 + `nanoammeter_capture.py` 采的）。
采集条件：`NPLC=10`、`autozero=ON`、`limitv=5V`。

三常数拟合（MATLAB 与独立 Python 实现结果一致）：

| | 本次 | 旧（09-13） | 差 |
|---|---|---|---|
| `q` | 1.559440e-14 | 1.5546e-14 | +0.31% |
| `I₊` | +49.7153 nA | +49.7172 nA | −0.004% |
| `I₋` | −49.7423 nA | −49.7428 nA | −0.001% |
| RMSE | 1.4093 pA | 1.50 pA | 更好 |
| `cond(A)` | 4.6 | 7.39 | 更好 |

**物理常数复现到 0.3% 以内** → 装置可复现。

未校准准确度：**+1.04% ± 0.04%**（18 个非零点全在 +0.99%~+1.12%），是全域恒定的
纯增益误差，与旧数据的 +1.0~+1.3% 吻合。

### ⚠️ 线性度必须报**样本外**的

> **2026-09-15 更正**：最初报的是 "0.00252 % FS"，那是**样本内残差** —— 拟合用的
> 3 个参数就是照着同一组 57 行调出来的，拿它的残差当线性度是**循环论证**。
> 用户当场指出"标定组和验证组要分开"。

重做（同一批数据，事后拆分即可）：

| 方法 | 样本外 max\|残差\| | 线性度 |
|---|---|---|
| 固定奇偶拆分（标定 10 点 / 验证 9 点） | 3.540 pA | 0.00393 % FS |
| **留一交叉验证（逐点）** | **3.813 pA** | **0.00424 % FS** |
| ~~样本内（作废）~~ | ~~2.266 pA~~ | ~~0.00252 % FS~~ |

**可引用值：线性度 ≈ 0.0042 % FS（≈ 3.8 pA / 90 nA）**，比旧标定 0.0137% 好约 **3.3 倍**
（不是最初说的 5 倍——那个倍数也是样本内的虚高）。

固定拆分那组的残差**随电流单调漂移**（−40 nA 处 −3.5 pA，+40 nA 处 +3.3 pA），
是两组之间约 1e-5 的增益差，属任意拆分的产物；留一验证没有这个趋势，更可信。

零点偏置 0.62 pA。

### 已知弱点：`q` 的标准误偏大

`fit_constants.m` 的杠杆分析：端点项跨度仅 869 pA，计数项 89400 pA，
**杠杆比 103:1** → `M(q)` 相对标准误 **0.073%**，比 `I±` 的 0.001% 大 70 倍。
根因：窗口固定 6250 拍、锯齿周期由电流决定 → 终点相位 = `6250 mod 周期`，
**同一电流重复测相位相同**，重复测量只压噪声、不加端点跨度。

q 的误差是纯增益（进 a），**不影响线性度**。要改善得改用**开环**测量
（固定参考极性 + 灌已知电流 + 让 Δc 跑满斜坡），注意**不能靠"参考断开"**——
`EN=0` 是 Hi-Z 不是 0 A。

---

## 三、关键结论（详见根 README 与 docs/）

- **未校准准确度**：+1.0 ~ +1.3%（纯增益误差 + 约 1.3 pA 偏置）
- **线性度**：0.0137 % FS（≈ 11 pA / 80 nA）—— 比准确度好约 **100 倍**
- **两路 ADC**（内置 vs ADS8866）：斜率差 2 ppm，**换 ADS8866 对准确度没有帮助**
- **三常数拟合**：`q = 1.5546e-14 C/码`、`I₊ = +49.7172 nA`、`I₋ = −49.7428 nA`
  （标准误 0.002%），RMSE 1.50 pA，`cond(A) = 7.39`
- **交叉验证差 1.02%**：由拟合值反推 `R = 100.51 MΩ`，万用表读 99.48 MΩ ——
  差值落在万用表 100 MΩ 档 ±1% 的边缘，在不确定度之内
- **`EN=0` 的 Hi-Z 暂态是真的**：`C_p ≈ 4.9 pF`、`τ ≈ 490 µs`、`Q ≈ 24 pC/次`
  （示波器看到的 ms 级指数衰减主要是**探头自己的 10 pF** 造成的）

## 四、复现

```bash
# 标定点采集（每个已知电流跑一次）
python tools/nanoammeter_capture.py COM8 --no-noise --true=<nA>

# 汇总成拟合用的 CSV
python tools/make_fit_csv.py -d data -o data/fit_data.csv

# 三常数最小二乘
bash tools/matlab_utf8.sh "cd('tools'); fit_constants('../data/fit_data.csv')"

# 线性度与图 4-2
python tools/fit_linearity.py -d data --plot
```
